


    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }

        return this;
    }


D:\_python\clone\CloneClassification\data_new\android\GoogleMapsV2WithActionBarSherlock\actionbarsherlock\src\com\actionbarsherlock\internal\view\menu\MenuItemImpl.java 10533
========================================================

    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }

        return this;
    }


D:\_python\clone\CloneClassification\data_new\android\AndroidTraining\Eclipse\libraries\actionbarsherlock\src\com\actionbarsherlock\internal\view\menu\MenuItemImpl.java 25363
========================================================

    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }

        return this;
    }


D:\_python\clone\CloneClassification\data_new\android\little-bear-dictionary\libraries\ActionbarSherlockLibrary\src\com\actionbarsherlock\internal\view\menu\MenuItemImpl.java 38072
========================================================

    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }

        return this;
    }


D:\_python\clone\CloneClassification\data_new\android\ActionBarSherlock\actionbarsherlock\src\com\actionbarsherlock\internal\view\menu\MenuItemImpl.java 87156
========================================================


    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }

        return this;
    }


D:\_python\clone\CloneClassification\data_new\android\Superuser\src\com\actionbarsherlock\internal\view\menu\MenuItemImpl.java 90295
========================================================

    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }

        return this;
    }


D:\_python\clone\CloneClassification\data_new\android\cw-omnibus\external\ActionBarSherlock\src\com\actionbarsherlock\internal\view\menu\MenuItemImpl.java 101410
========================================================

    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }
        
        return this;
    }


D:\_python\clone\CloneClassification\data_new\android\XobotOS\android\upstream\com\android\internal\view\menu\MenuItemImpl.java 129155
========================================================

    public MenuItem setChecked(boolean checked) {
        if ((mFlags & EXCLUSIVE) != 0) {
            // Call the method on the Menu since it knows about the others in this
            // exclusive checkable group
            mMenu.setExclusiveItemChecked(this);
        } else {
            setCheckedInt(checked);
        }

        return this;
    }


D:\_python\clone\CloneClassification\data_new\network\networklog\ActionBarSherlock\library\src\com\actionbarsherlock\internal\view\menu\MenuItemImpl.java 332611
========================================================
